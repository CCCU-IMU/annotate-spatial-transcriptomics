# v1.9.2 — Frozen-scope and state-schema compatibility

- Correctly validate full-object membership freezes that include analysis and initial-QC-excluded observations.
- Normalize supported descriptive mapping states into the canonical ledger enum and reject unknown states before writing.
